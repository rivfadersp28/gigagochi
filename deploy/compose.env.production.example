WEBAPP_HOST=gigagochi.serega.works
PUBLIC_PROXY_NETWORK=bizzy-radio_default

# Only needed when running the optional legacy-db profile.
POSTGRES_USER=tamagotchi
POSTGRES_PASSWORD=change-me
POSTGRES_DB=tamagotchi
